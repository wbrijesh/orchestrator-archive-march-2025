const { Loader } = require("lucide-react")

const LoadingSpinner = () => {
    return (
        <>
        <Loader className="animate-spin text-neutral-500"></Loader>
        </>
    )
}

export default LoadingSpinner